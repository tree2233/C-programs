#include<stdio.h>
void main()
{
    int l,b,area;
    printf("Enter the length and breath of the Rectangle:");
    scanf("%d %d",&l,&b);
    area=l*b;
    printf("Area of the Rectangle is %d square units.\n",area);
    printf("Thank you.\n");
}
